apiVersion: v1
kind: Service
metadata:
  name: {{ template "bwt_filtering_pipeline_chart.fullname" . }}
  labels:
    app: {{ template "bwt_filtering_pipeline_chart.name" . }}
    chart: {{ template "bwt_filtering_pipeline_chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "bwt_filtering_pipeline_chart.name" . }}
    release: {{ .Release.Name }}
